# Exclude screenshot-service from Vercel builds
screenshot-service/

# Note: scripts/ directory is included so copy-prisma-to-next.js is available during build

# Exclude development files
*.md
docs/
.git/
.gitignore

# Exclude test files
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx

# Exclude database files (already handled by gitignore, but extra safety)
*.db
*.db-journal
*.db.backup*

# Exclude local env files
.env.local
.env*.local

